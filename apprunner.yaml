version: 1.0

# Build settings
build:
  commands:
    - pip install -r requirements.txt

# Runtime settings
runtime:
  type: Python
  version: 3.8

# Command to start the application
start:
  command: sh startup.sh
  port: 8080
